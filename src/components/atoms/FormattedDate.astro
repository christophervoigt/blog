---
const { date } = Astro.props
---

<time datetime={new Date(date).toISOString()}>
	{
		new Date(date).toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>

<style>
	time {
		flex: 0 0 130px;
	}
</style>
