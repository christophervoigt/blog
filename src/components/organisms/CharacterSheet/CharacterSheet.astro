---
import './CharacterSheet.css'

interface Props {
	name: string
	class: string
	subclass?: string
	level: number
	hit_points: number
	armor_class: number
	speed: string
	proficiency_bonus: number
	initiative: number
	abilities: Record<string, number>
	saving_throws: Record<string, { base_value: number; proficiency?: boolean }>
	senses?: {
		passive_perception?: number
		darkvision?: string
		blindsight?: string
	}
	defenses?: Record<string, string[]>
	training: Record<string, string[]>
	skills: Record<string, { base_value: number; proficiency?: boolean; disadvantage?: boolean }>
	background?: {
		title: string
		feature: string
		description: string
	}
	characteristics?: {
		race?: string
		alignment?: string
		gender?: string
		size?: string
		eyes?: string
		hair?: string
		personality_traits?: string
		ideals?: string
		bonds?: string
		flaws?: string
	}
	actions?: {
		note?: string
		attacks: { name: string; meta?: string; to_hit?: string; damage?: string }[]
		footnote?: string
	}
	bonus_actions?: { type?: 'Bonus' | 'Reaction'; name: string; text: string }[]
	inventory?: { group: string; items: { name: string; qty?: number; note?: string }[] }[]
	coins?: { gold?: number; silver?: number; copper?: number }
	resources?: { name: string; reset?: string; max: number; current?: number }[]
	features?: { source?: string; name: string; meta?: string; text: string }[]
	feats?: { level?: string; name: string; text: string }[]
}

const {
	name,
	class: charClass,
	subclass,
	level,
	hit_points,
	armor_class,
	speed,
	proficiency_bonus,
	initiative,
	abilities,
	saving_throws,
	senses,
	defenses,
	training,
	skills,
	background,
	characteristics,
	actions,
	bonus_actions,
	inventory,
	coins,
	resources,
	features,
	feats,
} = Astro.props

function modifier(score: number) {
	const mod = Math.floor((score - 10) / 2)
	return mod >= 0 ? `+${mod}` : `${mod}`
}

function formatSkillName(key: string) {
	return key
		.split('_')
		.map((word) => word.charAt(0).toUpperCase() + word.slice(1))
		.join(' ')
}

function formatModifier(value: number) {
	return value >= 0 ? `+${value}` : `${value}`
}

function slugify(value: string) {
	return value
		.toLowerCase()
		.replace(/[^a-z0-9]+/g, '-')
		.replace(/^-|-$/g, '')
}

function formatSavingThrow(save: { base_value: number; proficiency?: boolean }) {
	const total = save.base_value + (save.proficiency ? proficiency_bonus : 0)
	return formatModifier(total)
}

function skillModifier(skill: { base_value: number; proficiency?: boolean }) {
	const total = skill.base_value + (skill.proficiency ? proficiency_bonus : 0)
	return total >= 0 ? `+${total}` : `${total}`
}

const abilityOrder = ['strength', 'dexterity', 'constitution', 'intelligence', 'wisdom', 'charisma']
const abilityAbbr: Record<string, string> = {
	strength: 'STR',
	dexterity: 'DEX',
	constitution: 'CON',
	intelligence: 'INT',
	wisdom: 'WIS',
	charisma: 'CHA',
}

const skillAbilityMap: Record<string, string> = {
	acrobatics: 'dexterity',
	animal_handling: 'wisdom',
	arcana: 'intelligence',
	athletics: 'strength',
	deception: 'charisma',
	history: 'intelligence',
	insight: 'wisdom',
	intimidation: 'charisma',
	investigation: 'intelligence',
	medicine: 'wisdom',
	nature: 'intelligence',
	perception: 'wisdom',
	performance: 'charisma',
	persuasion: 'charisma',
	religion: 'intelligence',
	sleight_of_hand: 'dexterity',
	stealth: 'dexterity',
	survival: 'wisdom',
}

const classLine = [subclass, charClass].filter(Boolean).join(' ')
const characterSubtitle = [`Level ${level}`, characteristics?.race, classLine].filter(Boolean).join(' ')

const hasNotes = Astro.slots.has('notes')

const storageKey = `cs-state:${slugify(name)}`
---

<div class="character-sheet" data-cs-key={storageKey}>
	<!-- Header -->
	<div class="cs-header -highlight">
		<div class="cs-header-id">
			<h1 class="cs-name">{name}</h1>
			<p class="cs-subtitle">{characterSubtitle}</p>
		</div>

		<div class="cs-hp" data-cs-hp-max={hit_points}>
			<span class="cs-hp-title">Hit Points</span>
			<p class="cs-hp-values">
				<span class="cs-hp-cur" data-cs-hp="cur">{hit_points}</span>
				<span class="cs-hp-max">/ <span data-cs-hp="max">{hit_points}</span></span>
				<span class="cs-hp-temp" data-cs-hp="temp" hidden>+<span data-cs-hp="temp-val">0</span> temp</span>
			</p>
			<input
				type="range"
				class="cs-hp-range"
				data-cs-hp="range"
				data-state="healthy"
				min="0"
				max={hit_points}
				value={hit_points}
				style="--hp-pct: 100%;"
				aria-label="Current hit points"
			/>
			<div class="cs-hp-controls">
				<div class="cs-hp-group">
					<button type="button" class="cs-hp-btn -dmg" data-cs-hp-btn="damage" aria-label="Take 1 damage">−</button>
					<span class="cs-hp-group-label">HP</span>
					<button type="button" class="cs-hp-btn -heal" data-cs-hp-btn="heal" aria-label="Heal 1 hit point">+</button>
				</div>
				<div class="cs-hp-group">
					<button type="button" class="cs-hp-btn -temp" data-cs-hp-btn="temp-down" aria-label="Decrease temporary hit points">−</button>
					<span class="cs-hp-group-label">Temp</span>
					<button type="button" class="cs-hp-btn -temp" data-cs-hp-btn="temp-up" aria-label="Increase temporary hit points">+</button>
				</div>
			</div>
		</div>

		<div class="cs-rests">
			<button type="button" class="cs-rest" data-cs-rest="short rest">Short Rest</button>
			<button type="button" class="cs-rest" data-cs-rest="long rest">Long Rest</button>
		</div>
	</div>

	<!-- Core Stats Bar -->
	<div class="cs-core-stats">
		<div class="cs-stat">
			<span class="cs-stat-value">{armor_class}</span>
			<span class="cs-stat-label">Armor Class</span>
		</div>
		<div class="cs-stat">
			<span class="cs-stat-value">{speed}</span>
			<span class="cs-stat-label">Speed</span>
		</div>
		<div class="cs-stat">
			<span class="cs-stat-value">{formatModifier(initiative)}</span>
			<span class="cs-stat-label">Initiative</span>
		</div>
		<div class="cs-stat">
			<span class="cs-stat-value">+{proficiency_bonus}</span>
			<span class="cs-stat-label">Proficiency</span>
		</div>
	</div>

	<!-- Body: three columns -->
	<div class="cs-body">
		<!-- Left column -->
		<div class="cs-col -left">
			<!-- Abilities -->
			<div class="cs-abilities-section">
				<h2 class="cs-section-title">Abilities</h2>
				<div class="cs-abilities">
					{
						abilityOrder.map((key) => (
							<div class="cs-ability">
								<span class="cs-ability-mod">{modifier(abilities[key])}</span>
								<span class="cs-ability-name">{abilityAbbr[key]}</span>
							</div>
						))
					}
				</div>
			</div>

			<!-- Saving Throws -->
			<section class="cs-saves">
				<h2 class="cs-section-title">Saving Throws</h2>
				<ul class="cs-list">
					{
						abilityOrder.map((key) => {
							const save = saving_throws[key]
							const proficient = save.proficiency

							return (
								<li class:list={[{ 'cs-proficient -highlight': proficient }]}>
									<span class="cs-list-label">
										{proficient && <span class="sr-only">Proficient in </span>}
										{formatSkillName(key)}
									</span>
									<span class="cs-list-value">{formatSavingThrow(save)}</span>
								</li>
							)
						})
					}
				</ul>
			</section>

			<!-- Skills -->
			<section class="cs-skills">
				<h2 class="cs-section-title">Skills</h2>
				<ul class="cs-list">
					{
						Object.entries(skills).map(([key, skill]) => {
							const total = skillModifier(skill)
							const proficient = skill.proficiency
							const disadvantaged = skill.disadvantage

							return (
								<li
									class:list={[{ 'cs-disadvantage -highlight': disadvantaged, 'cs-proficient -highlight': proficient }]}
								>
									<span class="cs-list-label">
										{proficient && <span class="sr-only">Proficient in </span>}
										{disadvantaged && <span class="sr-only">Disadvantaged in </span>}
										{formatSkillName(key)}
									</span>
									<span class="cs-list-value">{total}</span>
								</li>
							)
						})
					}
				</ul>
			</section>

			<!-- Senses -->
			{
				senses && (
					<section class="cs-senses-section">
						<h2 class="cs-section-title">Senses</h2>
						<ul class="cs-senses">
							{senses.passive_perception !== undefined && (
								<li>
									<p>{senses.passive_perception}</p>
									<p>Passive Perception</p>
								</li>
							)}
							{senses.darkvision && (
								<li>
									<p>{senses.darkvision}</p>
									<p>Darkvision</p>
								</li>
							)}
							{senses.blindsight && (
								<li>
									<p>{senses.blindsight}</p>
									<p>Blindsight</p>
								</li>
							)}
						</ul>
					</section>
				)
			}

			<!-- Defenses -->
			{
				defenses && Object.entries(defenses).some(([, v]) => v.length > 0) && (
					<section class="cs-defenses-section">
						<h2 class="cs-section-title">Defenses</h2>
						{Object.entries(defenses)
							.filter(([, items]) => items.length > 0)
							.map(([category, items]) => (
								<div class="cs-defenses-group">
									<h3 class="cs-defenses-label">{formatSkillName(category)}</h3>
									<ul class="cs-tags">
										{items.map((item) => (
											<li>{item}</li>
										))}
									</ul>
								</div>
							))}
					</section>
				)
			}

			<!-- Training / Proficiencies -->
			<section class="cs-proficiencies">
				<h2 class="cs-section-title">Proficiencies</h2>
				{
					Object.entries(training).map(([category, items]) => (
						<div class="cs-training-group">
							<h3 class="cs-training-label">{formatSkillName(category)}</h3>
							<ul class="cs-tags">
								{items.map((item) => (
									<li>{item}</li>
								))}
							</ul>
						</div>
					))
				}
			</section>
		</div>

		<!-- Middle column -->
		<div class="cs-col -main">
			<!-- Actions / Attacks -->
			{
				actions && (
					<section class="cs-actions">
						<h2 class="cs-section-title">Actions</h2>
						{actions.note && <p class="cs-note">{actions.note}</p>}
						<div class="cs-attacks">
							<p class="cs-attacks-head">Weapon</p>
							<p class="cs-attacks-head -center">To Hit</p>
							<p class="cs-attacks-head -center">Damage</p>
							{actions.attacks.map(({ name, meta, to_hit, damage }) => (
								<Fragment>
									<div class="cs-attack-name">
										<p class="cs-attack-title">{name}</p>
										{meta && <p class="cs-attack-meta">{meta}</p>}
									</div>

									<p class="cs-attack-value -tohit">{to_hit}</p>
									<p class="cs-attack-value">{damage}</p>
								</Fragment>
							))}
						</div>
					</section>
				)
			}

			<!-- Bonus Actions & Reactions -->
			{
				bonus_actions && bonus_actions.length > 0 && (
					<section class="cs-tempo">
						<h2 class="cs-section-title">Bonus Actions & Reactions</h2>
						<ul class="cs-tempo-list">
							{bonus_actions.map((action) => (
								<li class="cs-tempo-item">
									<div class="cs-tempo-head">
										<p class:list={['cs-tag', { '-reaction': action.type === 'Reaction' }]}>{action.type ?? 'Bonus'}</p>
										<p class="cs-tempo-name">{action.name}</p>
									</div>
									<p class="cs-tempo-text">{action.text}</p>
								</li>
							))}
						</ul>
					</section>
				)
			}
			<!-- Class & Racial Features -->
			{
				features && features.length > 0 && (
					<section class="cs-features">
						<h2 class="cs-section-title">Class & Racial Features</h2>
						<ul class="cs-feature-list">
							{features.map((feature) => (
								<li class="cs-feature">
									<div class="cs-feature-head">
										{feature.source && (
											<p
												class:list={[
													'cs-tag',
													{ '-subclass': feature.source === subclass },
													{ '-race': feature.source === characteristics?.race },
												]}
											>
												{feature.source}
											</p>
										)}
										<p class="cs-feature-name">{feature.name}</p>
									</div>
									<p class="cs-feature-text">{feature.text}</p>
								</li>
							))}
						</ul>
					</section>
				)
			}

			<!-- Feats -->
			{
				feats && feats.length > 0 && (
					<section class="cs-feats">
						<h2 class="cs-section-title">Feats</h2>
						<ul class="cs-feat-list">
							{feats.map((feat) => (
								<li class="cs-feat">
									<div class="cs-feat-head">
										{feat.level && <p class="cs-feat-level">{feat.level}</p>}
										<p class="cs-feat-name">{feat.name}</p>
									</div>
									<p class="cs-feat-text">{feat.text}</p>
								</li>
							))}
						</ul>
					</section>
				)
			}
		</div>

		<!-- Right column -->
		<div class="cs-col -right">
			<!-- Death Saves -->
			<section class="cs-deathsaves">
				<h2 class="cs-section-title">Death Saves</h2>

				<div class="cs-deathsave-wrapper">
					<div class="cs-deathsave-group">
						<p class="cs-deathsave-label">
							Successes <span class="sr-only" data-cs-count="deathS" data-cs-count-label="marked">0 of 3 marked</span>
						</p>
						<div class="cs-pips">
							{
								Array.from({ length: 3 }).map((_, i) => (
									<button
										type="button"
										class="cs-pip -success"
										data-cs-pip
										data-cs-group="deathS"
										data-cs-index={i}
										aria-pressed="false"
										aria-label={`Death save success ${i + 1}`}
									/>
								))
							}
						</div>
					</div>

					<div class="cs-deathsave-group">
						<p class="cs-deathsave-label">
							Failures <span class="sr-only" data-cs-count="deathF" data-cs-count-label="marked">0 of 3 marked</span>
						</p>
						<div class="cs-pips -failure">
							{
								Array.from({ length: 3 }).map((_, i) => (
									<button
										type="button"
										class="cs-pip"
										data-cs-pip
										data-cs-group="deathF"
										data-cs-index={i}
										aria-pressed="false"
										aria-label={`Death save failure ${i + 1}`}
									/>
								))
							}
						</div>
					</div>
				</div>
			</section>

			<!-- Resources -->
			{
				resources && resources.length > 0 && (
					<section class="cs-resources">
						<h2 class="cs-section-title">Resources</h2>
						<div class="cs-resource-grid">
							<p class="cs-resource-head">Resource</p>
							<p class="cs-resource-head -right">Uses</p>
							{resources.map((resource) => {
								const resGroup = `res-${slugify(resource.name)}`
								return (
									<Fragment>
										<div class="cs-resource-name">
											<p class="cs-resource-title">
												{resource.name}
												<span class="sr-only" data-cs-count={resGroup} data-cs-count-label="used">
													- 0 of {resource.max} used
												</span>
											</p>
											{resource.reset && <p class="cs-resource-reset">{resource.reset}</p>}
										</div>

										<div class="cs-pips -resource">
											{Array.from({ length: resource.max }).map((_, i) => (
												<button
													type="button"
													class="cs-pip"
													data-cs-pip
													data-cs-group={resGroup}
													data-cs-index={i}
													data-cs-reset={resource.reset}
													aria-pressed="false"
													aria-label={`${resource.name} use ${i + 1}`}
												/>
											))}
										</div>
									</Fragment>
								)
							})}
						</div>
					</section>
				)
			}

			<!-- Inventory -->
			{
				(inventory || coins) && (
					<section class="cs-inventory">
						<h2 class="cs-section-title">Inventory</h2>
						{inventory?.map((group) => (
							<div class="cs-inv-group">
								<h3 class="cs-inv-label">{group.group}</h3>
								<ul class="cs-inv-list">
									{group.items.map((item) => (
										<li class="cs-inv-item">
											<p class="cs-inv-name">{item.name}</p>
											{item.note && <p class="cs-inv-note">{item.note}</p>}
										</li>
									))}
								</ul>
							</div>
						))}
						{coins && (
							<div class="cs-coins">
								{coins.gold !== undefined && (
									<div class="cs-coin -gold">
										<p class="cs-coin-value">{coins.gold}</p>
										<p class="cs-coin-label">Gold</p>
									</div>
								)}
								{coins.silver !== undefined && (
									<div class="cs-coin -silver">
										<p class="cs-coin-value">{coins.silver}</p>
										<p class="cs-coin-label">Silver</p>
									</div>
								)}
								{coins.copper !== undefined && (
									<div class="cs-coin -copper">
										<p class="cs-coin-value">{coins.copper}</p>
										<p class="cs-coin-label">Copper</p>
									</div>
								)}
							</div>
						)}
					</section>
				)
			}

			<!-- Background -->
			{
				background && (
					<section class="cs-background">
						<h2 class="cs-section-title">Background</h2>
						<div class="cs-background-head">
							<p class="cs-tag">{background.title}</p>
							<p class="cs-background-name">{background.feature}</p>
						</div>
						<p class="cs-background-desc">{background.description}</p>
					</section>
				)
			}

			<!-- Characteristics -->
			{
				characteristics && (
					<section class="cs-characteristics">
						<h2 class="cs-section-title">Characteristics</h2>

						<div class="cs-physical">
							{characteristics.alignment && (
								<div class="cs-physical-item">
									<p class="cs-trait-label">Alignment</p>
									<p class="cs-physical-item-value">{characteristics.alignment}</p>
								</div>
							)}
							{characteristics.race && (
								<div class="cs-physical-item">
									<p class="cs-trait-label">Race</p>
									<p class="cs-physical-item-value">{characteristics.race}</p>
								</div>
							)}
							{characteristics.gender && (
								<div class="cs-physical-item">
									<p class="cs-trait-label">Gender</p>
									<p class="cs-physical-item-value">{characteristics.gender}</p>
								</div>
							)}
							{characteristics.size && (
								<div class="cs-physical-item">
									<p class="cs-trait-label">Size</p>
									<p class="cs-physical-item-value">{characteristics.size}</p>
								</div>
							)}
							{characteristics.eyes && (
								<div class="cs-physical-item">
									<p class="cs-trait-label">Eyes</p>
									<p class="cs-physical-item-value">{characteristics.eyes}</p>
								</div>
							)}
							{characteristics.hair && (
								<div class="cs-physical-item">
									<p class="cs-trait-label">Hair</p>
									<p class="cs-physical-item-value">{characteristics.hair}</p>
								</div>
							)}
						</div>

						<div class="cs-traits-grid">
							{characteristics.personality_traits && (
								<div class="cs-trait">
									<p class="cs-trait-label">Personality Traits</p>
									<p class="cs-trait-value">{characteristics.personality_traits}</p>
								</div>
							)}
							{characteristics.ideals && (
								<div class="cs-trait">
									<p class="cs-trait-label">Ideals</p>
									<p class="cs-trait-value">{characteristics.ideals}</p>
								</div>
							)}
							{characteristics.bonds && (
								<div class="cs-trait">
									<p class="cs-trait-label">Bonds</p>
									<p class="cs-trait-value">{characteristics.bonds}</p>
								</div>
							)}
							{characteristics.flaws && (
								<div class="cs-trait">
									<p class="cs-trait-label">Flaws</p>
									<p class="cs-trait-value">{characteristics.flaws}</p>
								</div>
							)}
						</div>
					</section>
				)
			}
		</div>
	</div>

	<!-- Notes from Markdown Content -->
	{
		hasNotes && (
			<div class="cs-notes content">
				<slot name="notes" />
			</div>
		)
	}
</div>

<script>
	function initSheet(root) {
		const key = root.dataset.csKey
		const hpEl = root.querySelector('.cs-hp')
		const max = hpEl ? Number(hpEl.dataset.csHpMax) || 0 : 0

		// Discover pip groups (and their sizes) from the server-rendered DOM.
		const pips = Array.from(root.querySelectorAll('[data-cs-pip]'))
		const groupMax = {}
		const groupReset = {}
		for (const pip of pips) {
			const g = pip.dataset.csGroup
			groupMax[g] = (groupMax[g] || 0) + 1
			if (pip.dataset.csReset) groupReset[g] = pip.dataset.csReset
		}
		const isDeath = (g) => g === 'deathS' || g === 'deathF'

		// Default state mirrors the SSR baseline: full HP, whatever pips ship filled.
		const state = { hp: { cur: max, temp: 0 }, groups: {} }
		for (const g in groupMax) {
			state.groups[g] = pips.filter((p) => p.dataset.csGroup === g && p.classList.contains('-filled')).length
		}

		// Overlay any persisted state, clamped to the current maxima.
		try {
			const raw = key && localStorage.getItem(key)
			if (raw) {
				const saved = JSON.parse(raw)
				if (saved && saved.hp) {
					if (typeof saved.hp.cur === 'number') state.hp.cur = Math.max(0, Math.min(max, saved.hp.cur))
					if (typeof saved.hp.temp === 'number') state.hp.temp = Math.max(0, saved.hp.temp)
				}
				if (saved && saved.groups) {
					for (const g in groupMax) {
						const v = saved.groups[g]
						if (typeof v === 'number') state.groups[g] = Math.max(0, Math.min(groupMax[g], v))
					}
				}
			}
		} catch (e) {}

		function persist() {
			if (!key) return
			try {
				localStorage.setItem(key, JSON.stringify(state))
			} catch (e) {}
		}

		function render() {
			if (hpEl) {
				const { cur, temp } = state.hp
				const curEl = hpEl.querySelector('[data-cs-hp="cur"]')
				if (curEl) curEl.textContent = String(cur)
				const tempWrap = hpEl.querySelector('[data-cs-hp="temp"]')
				const tempVal = hpEl.querySelector('[data-cs-hp="temp-val"]')
				if (tempVal) tempVal.textContent = String(temp)
				if (tempWrap) tempWrap.hidden = temp <= 0
				const range = hpEl.querySelector('[data-cs-hp="range"]')
				if (range) {
					const pct = max > 0 ? Math.max(0, Math.min(100, Math.round((cur / max) * 100))) : 0
					range.value = String(cur)
					range.style.setProperty('--hp-pct', pct + '%')
					range.dataset.state = pct > 50 ? 'healthy' : pct > 25 ? 'wounded' : 'critical'
					range.setAttribute('aria-valuetext', `${cur} of ${max} hit points`)
				}
			}

			for (const pip of pips) {
				const filled = Number(pip.dataset.csIndex) < state.groups[pip.dataset.csGroup]
				pip.classList.toggle('-filled', filled)
				pip.setAttribute('aria-pressed', String(filled))
			}

			root.querySelectorAll('[data-cs-count]').forEach((el) => {
				const g = el.dataset.csCount
				const n = state.groups[g] || 0
				const m = groupMax[g] || 0
				el.textContent = el.dataset.csCountLabel === 'used' ? `- ${n} of ${m} used` : `${n} of ${m} marked`
			})
		}

		function setPip(g, i) {
			const cur = state.groups[g] || 0
			state.groups[g] = i + 1 === cur ? i : i + 1
		}

		function adjustHp(delta) {
			let { cur, temp } = state.hp
			if (delta < 0) {
				let d = -delta
				const absorbed = Math.min(temp, d)
				temp -= absorbed
				d -= absorbed
				cur = Math.max(0, cur - d)
			} else {
				cur = Math.min(max, cur + delta)
			}
			state.hp = { cur, temp }
		}

		// Resource pips track spent uses, so a rest clears them back to empty.
		function clearResources(resetType) {
			for (const g in groupMax) {
				if (isDeath(g)) continue
				if (resetType === null || groupReset[g] === resetType) state.groups[g] = 0
			}
		}

		function rest(kind) {
			if (kind === 'short rest') {
				clearResources('short rest')
			} else {
				clearResources(null)
				state.hp = { cur: max, temp: 0 }
				state.groups.deathS = 0
				state.groups.deathF = 0
			}
		}

		root.addEventListener('click', (event) => {
			const target = event.target
			const pip = target.closest('[data-cs-pip]')
			if (pip && root.contains(pip)) {
				setPip(pip.dataset.csGroup, Number(pip.dataset.csIndex))
				render()
				persist()
				return
			}
			const hpBtn = target.closest('[data-cs-hp-btn]')
			if (hpBtn) {
				const action = hpBtn.dataset.csHpBtn
				if (action === 'damage') adjustHp(-1)
				else if (action === 'heal') adjustHp(1)
				else if (action === 'temp-up') state.hp.temp += 1
				else if (action === 'temp-down') state.hp.temp = Math.max(0, state.hp.temp - 1)
				render()
				persist()
				return
			}
			const restBtn = target.closest('[data-cs-rest]')
			if (restBtn) {
				rest(restBtn.dataset.csRest)
				render()
				persist()
			}
		})

		// Dragging the range sets current HP directly (temp is unaffected).
		const range = hpEl && hpEl.querySelector('[data-cs-hp="range"]')
		if (range) {
			range.addEventListener('input', () => {
				state.hp.cur = Math.max(0, Math.min(max, parseInt(range.value, 10) || 0))
				render()
				persist()
			})
		}

		render()
	}

	function boot() {
		document.querySelectorAll('.character-sheet').forEach((el) => {
			if (el.dataset.csReady) return
			el.dataset.csReady = 'true'
			initSheet(el)
		})
	}

	boot()
	document.addEventListener('astro:page-load', boot)
</script>
